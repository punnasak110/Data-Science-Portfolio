# Thai Character Recognition using ResNet18 🇹🇭

โปรเจกต์นี้เป็นการพัฒนาระบบ Deep Learning เพื่อจำแนกตัวอักษรไทย (Thai Character Recognition) โดยใช้โมเดล **ResNet18** ที่มีการปรับแต่งและใช้เทคนิค **Data Augmentation** เพื่อเพิ่มประสิทธิภาพความแม่นยำสูงสุด

---

## 📂 โครงสร้างไฟล์ (File Structure)

รายชื่อไฟล์หลักในโปรเจกต์และหน้าที่ของแต่ละไฟล์:

### 1. `resnet_new.ipynb` (Model Training - Main File) 🧠
ไฟล์หลักฉบับสมบูรณ์สำหรับสร้างและเทรนโมเดล AI
* **Data Process:** โหลดข้อมูลรูปภาพตัวอักษรไทยและแบ่งข้อมูลเป็น Train/Validation อย่างถูกต้อง (แยก Transform ชัดเจน)
* **Augmentation:** ใช้เทคนิคขั้นสูง เช่น Rotation, Affine, Salt&Pepper Noise, Erosion/Dilation และ Font Style
* **Model:** สร้างโมเดลโดยใช้ **ResNet18** และเทรนด้วย Learning Rate `0.001`
* **Output:** มีระบบ **Save Best Model** จะบันทึกโมเดลเฉพาะเมื่อได้ค่าความแม่นยำสูงสุด (ได้ไฟล์ `resnet18_best_model.pth`)

### 2. `inference_DenseNet.ipynb` (Testing & Prediction) 🔮
ไฟล์สำหรับนำโมเดลที่เทรนเสร็จแล้วมาใช้งานจริง (Inference)
* โหลดโมเดลที่ดีที่สุดจากการเทรน
* อ่านรูปภาพจากโฟลเดอร์ `test` เพื่อทำการทำนายผล
* บันทึกผลลัพธ์เป็นไฟล์ CSV (`test_predictions.csv`)

### 3. `DS.ipynb` (Data Cleaning with FiftyOne) 🧹
ไฟล์สำหรับตรวจสอบคุณภาพข้อมูลก่อนนำไปเทรน
* ใช้เครื่องมือ **FiftyOne** (Visual Dataset) เพื่อหาภาพเสียหรือภาพที่อ่านไม่ออก (Tags: WTF, hardtosee)
* คัดรูปภาพที่มีปัญหาออกจาก Training Set

---

## 🛠 เทคนิคและเครื่องมือที่ใช้ (Methods & Tools)

* **Framework:** PyTorch
* **Model Architecture:** ResNet18 (Transfer Learning from ImageNet)
* **Data Cleaning:** FiftyOne
* **Augmentation Techniques:**
    * PadToSquare
    * RandomRotation
    * RandomFontStyle
    * Salt&Pepper Noise
    * RandomErosionDilation
* **Training Strategy:** Best Model Saving (Checkpointing)
